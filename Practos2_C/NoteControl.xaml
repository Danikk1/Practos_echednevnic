<UserControl x:Class="Practos2_C.NoteControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleTextBlock" Grid.Row="0" TextWrapping="Wrap" Margin="5" FontWeight="Bold" Text="{Binding Title}"/>
        <TextBlock x:Name="DescriptionTextBlock" Grid.Row="1" TextWrapping="Wrap" Margin="5" Text="{Binding Description}"/>
    </Grid>
</UserControl>
